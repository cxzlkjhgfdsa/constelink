spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/constelink_beneficiary
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: maria
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update